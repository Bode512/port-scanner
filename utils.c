/*
- get_timestamp() → devuelve string con fecha/hora actual
- ms_sleep() →
    #ifdef _WIN32
        - Sleep()
    #else
        - usleep()
    #endif
- str_trim() → quita espacios y \r\n de strings
- is_valid_ip() → valida formato de IP con regex simple
- progress_bar() → muestra % de puertos escaneados en terminal
*/